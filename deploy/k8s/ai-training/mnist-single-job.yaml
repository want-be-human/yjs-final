apiVersion: batch/v1
kind: Job
metadata:
  name: mnist-single
  namespace: default
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      imagePullSecrets:
        - name: swr-secret
      containers:
        - name: trainer
          image: swr.cn-north-4.myhuaweicloud.com/yjs-final/mnist-ddp:v3
          imagePullPolicy: IfNotPresent
          command:
            - python
            - train_single.py
            - --epochs
            - "2"
            - --batch-size
            - "128"
            - --data-dir
            - /work/data
            - --cpu
            - --allow-synthetic
            - --synthetic-size
            - "6000"
          resources:
            requests:
              cpu: "50m"
              memory: "256Mi"
            limits:
              cpu: "1"
              memory: "1536Mi"
